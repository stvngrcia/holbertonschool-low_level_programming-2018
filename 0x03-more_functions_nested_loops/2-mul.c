#include "holberton.h"
/**
 * mul - multiplies two integers.
 * @a: Multiplicant
 * @b: Multiplier
 * Return: Returns 1 if the letter is Uppercase 0 if lowercase
 */
int mul(int a, int b)
{
	return (a * b);
}
