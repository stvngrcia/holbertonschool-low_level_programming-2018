#include <stdlib.h>
#include <stdio.h>
/**
 * main - Entry point
 * Definition: Prints the name of the file that it was compiled from.
 * Return: Always 0
 */
int main(void)
{
	printf("%s\n", __FILE__);
	return (0);
}
